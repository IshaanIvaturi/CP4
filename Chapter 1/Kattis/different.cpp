#include <bits/stdc++.h>

using namespace std;

int main()
{
    //Initialize 2 long longs for big numbers
    long long a, b;

    //Keep taking in a and b
    while (cin >> a >> b)
    {
        //Output absolute value of difference
        cout << abs(a-b) << endl;
    }
}