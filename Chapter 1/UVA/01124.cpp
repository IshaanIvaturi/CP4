#include <bits/stdc++.h>

using namespace std;

int main()
{
    //Initialize string
    string s;

    //Keep taking in lines and outputting them exactly
    while (getline(cin, s))
    {
        cout << s << endl;
    }
}