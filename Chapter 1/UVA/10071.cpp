#include <bits/stdc++.h>

using namespace std;

int main()
{
    //Initialize v and t
    int v, t;

    //Keep taking in v and t when there are more
    while (cin >> v >> t)
    {
        //v*t = d, so simply output double that
        cout << 2*v*t << endl;
    }
}